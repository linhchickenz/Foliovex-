// Base URL for the Foliovex backend (Render-hosted production deployment).
// All API calls are prefixed with this so the frontend works regardless of
// where it's deployed (no reliance on a same-origin dev proxy).
export const API_BASE_URL = 'https://foliovex-backend.onrender.com'
